BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-henry"
BBFILE_PATTERN_meta-henry = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-henry = "6"

LAYERSERIES_COMPAT_meta-henry = "whinlatter"
